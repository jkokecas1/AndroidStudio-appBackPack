<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.shool.backpack">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".activities.BanderasActivity"></activity>
        <activity android:name=".activities.ContinentesActivity" />
        <activity android:name=".activities.GeoMexicoActivity" />
        <activity android:name=".activities.PersonajesActivity" />
        <activity
            android:name=".activities.ContenedorActivity"
            android:label="@string/title_activity_contenedor" />
        <activity android:name=".activities.CuentosActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".activities.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Fragments.Menu.MenuTablasFragment"
            tools:ignore="Instantiatable" />
    </application>

</manifest>